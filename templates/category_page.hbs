  <nav class="sub-nav">
    {{breadcrumbs}}
  </nav>
        <header class="page-header">
         		 <h1>{{category.name}}</h1>
     			  {{search submit=false}}
    		  </header>
<div class="container">
  <div class="category-container">

       <!-- levitt serial checker-->
<!--   <div id="tt_serial_indexer"> -->
    
    <!-- levitt: this is a small HIDEEN button used for import tt_product-->
<!--     <label class="axe zd_Hidden" id="axxe">import</label> -->
    <!-- levitt: this is tt_product JSON file, get the link in preview, inspect-->
<!--     <script> var assetsTtProduct = "{{asset 'tt_product.json'}}"; </script> -->
    <!-- levitt: this is a small HIDEEN button used for import tt_product-->
  
<!--     <h2>Check Your Serial Number</h2>  -->
<!--     <input type="text" id="tt_serial_no_input" class="tt_input" maxlength="2" /> -->
    
<!--     <div id="product_results_box" >
    <div class="single_result"></div>
    </div>
  
  </div> -->
  <!-- levitt serial checker-->
    



    <div class="category-content">

      
       <div class="section-tree">
        {{#each sections}}
          <section class="section">
            <h2 class="section-tree-title">
              <a href="{{url}}">{{name}}</a>
            </h2>
            {{#if articles}}
              <ul class="article-list">
                {{#each articles}}
                  <li class="article-list-item{{#if promoted}} article-promoted{{/if}}">
                    {{#if promoted}}
                      <span data-title="{{t 'promoted'}}" class="icon-star"></span>
                    {{/if}}
                    <a href="{{url}}" class="article-list-link">{{title}}</a>
                    {{#if internal}}
                      <span class="icon-lock" title="{{t 'internal'}}"></span>
                    {{/if}}
                  </li>
                {{/each}}
              </ul>
              {{#if more_articles}}
                <a href="{{url}}" class="see-all-articles">
                  {{t 'show_all_articles' count=article_count}}
                </a>
              {{/if}}
            {{/if}}
          </section>
        {{else}}
          <i class="category-empty">
            <a href="{{category.url}}">{{t 'empty'}}</a>
          </i>
        {{/each}}
       </div>
    </div>
  </div>
</div>
