<!-- Start of tomtomhelp1570114826 Zendesk Widget script -->
<script id="ze-snippet" src="https://static.zdassets.com/ekr/snippet.js?key=e99a4148-b9f9-4cf1-bb8e-9ed3aefc014a"> </script>
<!-- End of tomtomhelp1570114826 Zendesk Widget script -->

<!-- Start of hiding email and name on webwidget -->
<script type="text/javascript">
    zE('webWidget:on', 'open', function() { //on click of web widget
       var style = document.createElement('style'); //create style element

          style.innerHTML = `

            #Embed > div > div > div > form > div > div.sc-kaNhvL.iIpHXN > div:nth-child(1) {  
                display: none;     
              }
            #Embed > div > div > div > form > div > div.sc-kaNhvL.iIpHXN > div:nth-child(2) {   
                display: none;     
             }
                         `;

      parent.document.getElementById('webWidget').contentDocument.head.appendChild(style);
    });    
</script>
<!-- End  of hiding email and name on webwidget -->    
<nav class="sub-nav">
   <div class="container">
    {{breadcrumbs}}
   </div>
  </nav>
    <header class="page-header" id="accounts-page-header">
      <div class="container category-container">	
        <h1>{{category.name}}</h1>
       {{search submit=false class="search category-search" placeholder="Type keyword here"}}
      </div>
    </header>
<div class="category-body">
<div class="container">
  <div class="category-container">
    <div class="category-content">      
       <div class="section-tree">
        {{#each sections}}
          <section class="section">
            <h2 class="section-tree-title">
              <a href="{{url}}">{{name}}</a>
            </h2>
            {{#if articles}}
              <ul class="article-list">
                {{#each articles}}
                  <li class="article-list-item{{#if promoted}} article-promoted{{/if}}">
                    {{#if promoted}}
                      <span data-title="{{t 'promoted'}}" class="icon-star"></span>
                    {{/if}}
                    <a href="{{url}}" class="article-list-link">{{title}}</a>
                    {{#if internal}}
                      <span class="icon-lock" title="{{t 'internal'}}"></span>
                    {{/if}}
                  </li>
                {{/each}}
              </ul>
              {{#if more_articles}}
                <a href="{{url}}" class="see-all-articles button">
                  {{t 'show_all_articles' count=article_count}}
                </a>
              {{/if}}
            {{/if}}
          </section>
        {{else}}
          <i class="category-empty">
            <a href="{{category.url}}">{{t 'empty'}}</a>
          </i>
        {{/each}}
        {{#if promoted_articles}}
              <section class="articles promoted-articles-box">
                <div class="promoted_header"><h4>ANNOUNCEMENTS</h4><div class="close">X</div></div>
                <ul class="article-list promoted-articles">
                  {{#each promoted_articles}}
                    <li class="promoted-articles-item">
                        <a href="{{url}}">
                          {{title}}
                          {{#if internal}}
                            <span class="icon-lock" title="{{t 'internal'}}"></span>
                          {{/if}}
                          <div class="button promoted-articles-button">></div>
                        </a>
                    </li>
                  {{/each}}
                </ul>
              </section>
            {{/if}}
      </div>
    </div>
  </div>
</div>
</div>
